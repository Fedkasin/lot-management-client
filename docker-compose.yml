version: '3.5'

services:

  expo:
    container_name: expo
    image: usedcar-test
    build:
      context: .
      dockerfile: docker/mobilebuilder/Dockerfile
    env_file: .env
    ports:
     - 19000:19000
     - 19001:19001
     - 19002:19002
